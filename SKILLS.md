# QuickMeds Backend Skills

Use these Codex skills for repeatable QuickMeds backend workflows

| Skill               | Purpose |
|---------------------|---------|
| `backend-migration` | Continue the Nestar to QuickMeds backend modification while preserving the current NestJS architecture. |
| `product-logic`     | Review product GraphQL, DTO, Schema, enum, filter and naming consistency. |

Each skill lives at `skills/<skill-name>/SKILL.md`.